{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:lib": "lerna run build:lib",
    "publish:lib": "lerna publish --force-publish=@seven/theme --no-git-tag-version --no-push",
    "build::publish": "yarn build && yarn publish"
  },
  "devDependencies": {
    "lerna": "^5.2.0"
  },
  "dependencies": {
    "@types/node": "^18.6.2",
    "typescript": "^4.7.4"
  }
}